<%= form_for @todo do |f| %>
	<%= label_tag(:title, "title") %>
	<%= f.text_field :title %>

	<br>
	<br>

	<%= label_tag(:description, "description") %>
	<%= f.text_area :description %>

	<%= f.submit "submit" %>
<% end %>